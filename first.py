# โปรแกรมคำนวณพื้นที่สี่เหลี่ยมผืนผ้า

width = float(input("กรอกความกว้าง: "))
length = float(input("กรอกความยาว: "))

area = width * length

print("\nผลการคำนวณ")
print(f"ความกว้าง = {width}")
print(f"ความยาว = {length}")
print(f"พื้นที่ = {area}")